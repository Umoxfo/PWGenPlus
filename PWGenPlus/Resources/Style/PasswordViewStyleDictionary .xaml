<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    mc:Ignorable="d">
    <DataTemplate x:Key="PasswordViewTemplate">
        <Border BorderBrush="Black" Margin="8" Padding="4" BorderThickness="1">
            <Grid MinWidth="14" MinHeight="22">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Task Name:"/>
                <!--<TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Path=TaskName}" />-->
                <TextBlock Grid.Row="1" Text="Description:"/>
                <!--<TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Path=Description}"/>-->
                <TextBlock Grid.Row="2" Text="Priority:"/>
                <!--<TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Path=Priority}"/>-->
            </Grid>
        </Border>
    </DataTemplate>
</ResourceDictionary>
